import 'package:flutter/material.dart';

class FindNotes extends StatelessWidget {
  const FindNotes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Find Notes"),),
    );
  }
}