import 'package:flutter/material.dart';

class FindSubjects extends StatelessWidget {
  const FindSubjects({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Column(
          children: [Text("Settings page.")],
        ),
      ),
    );
  }
}
